import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  //TODO burayı doldur
};

createSlice({
  name: "categorySlice",
  initialState,
  reducers: {
    setCategories: (state, action) => {},
  },
});
